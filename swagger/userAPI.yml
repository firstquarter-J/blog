paths:
 /register:
  get:
     tags:
     - 회원가입
     description: 모든 메뉴 조회
     produces:
     - application/json
     parameters:
        - in: query
          name: category1
          required: true
          schema:
            type: integer
            description: 카테고리1
        - in: query
          name: category2
          required: true
          schema:
            type: integer
            description: 카테고리2
        - in: query
          name: category3
          required: true
          schema:
            type: integer
            description: 카테고리3
     responses:
      200:
       description: 메뉴 조회 성공
  post:
    summary: create a menu